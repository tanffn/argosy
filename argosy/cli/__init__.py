"""Argosy CLI (Phase 1).

Subcommands:

  argosy intake       — run the intake interview interactively
  argosy critique     — run plan-critique against latest snapshot
  argosy ingest tsv   — import a Family Finances Status TSV
  argosy ingest plan  — import a plan markdown document
  argosy secrets set  — store an Argosy secret in the OS keychain
  argosy secrets get  — read an Argosy secret from the OS keychain
"""

from __future__ import annotations
